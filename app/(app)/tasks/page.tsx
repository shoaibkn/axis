export default function TasksPage() {
  return (
    <main className="mx-auto min-h-screen w-full max-w-5xl px-6 py-10">
      <h1 className="text-2xl font-semibold">Tasks</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Demo page: this is where task lists, statuses, priorities, and assignees will live.
      </p>
      <div className="mt-6 rounded-lg border border-border p-4 text-sm text-muted-foreground">
        Coming next: task templates, assignments, due dates, and SLA tracking.
      </div>
    </main>
  );
}
